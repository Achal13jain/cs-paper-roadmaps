name: "✅ Validate Contribution"

on:
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: python -m pip install --upgrade pyyaml jsonschema requests

      - name: Validate papers.yml
        run: python scripts/validate_papers.py

      - name: Fetch base branch for changed-link detection
        run: git fetch origin ${{ github.event.pull_request.base.ref }} --depth=1

      - name: Check new or changed paper links
        run: python scripts/check_links.py --changed-only --base-ref origin/${{ github.event.pull_request.base.ref }}
